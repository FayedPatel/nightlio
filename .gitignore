# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
docker-compose.override.yml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python (legacy, kept for stray local artifacts)
venv/
.venv/
instance/
*.pyc
*.pyo
*.pyd
__pycache__/

# Database
*.db
*.sqlite
*.sqlite3
data

# Pytest
*.pytest_cache/

# Prod Test
docker-compose.prod.yml
Caddyfile
old

# Personal server-admin scripts — local only, not part of the public repo
scripts/upgrade-server.sh
scripts/relink-oidc-user.sh
scripts/fix-group-ownership.sh

# Claude Code working files — keep local only
.claude/
CLAUDE.md
PLAN.md
BASELINE.md
plan-2.md

# Local carrier archive — never commit
project.zip

# Test artifacts
playwright-report/
test-results/
coverage/
screenshots/

# Rust API rewrite
api/target/

# Contract corpus: the committed golden .db snapshots must override the
# global *.db ignore above — cargo's bootstrap-parity tests read them.
# Real production snapshots stay ignored (personal data, never commit).
!contract/corpus/*.db
contract/corpus/prod/