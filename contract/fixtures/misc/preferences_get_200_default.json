{
  "method": "GET",
  "path": "/api/preferences",
  "request": {
    "headers_that_matter": {
      "Authorization": "Bearer <JWT>"
    },
    "body": null
  },
  "response": {
    "status": 200,
    "content_type": "application/json",
    "body": {
      "theme": null
    }
  },
  "note": "Fresh user: users.theme_preference is a TEXT column with NO default, so theme is null until the first successful PUT. The frontend must treat null as the 'default' theme; the string 'default' is only ever stored after an explicit PUT."
}
