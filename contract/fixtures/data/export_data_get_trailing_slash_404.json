{
  "method": "GET",
  "path": "/api/export/data/",
  "request": {
    "headers_that_matter": {
      "Authorization": "Bearer <JWT>"
    },
    "body": null
  },
  "response": {
    "status": 404,
    "content_type": "application/json",
    "body": {
      "error": "Resource not found"
    }
  },
  "note": "NEW FAMILY (v0.6.0, hand-authored spec fixture — Rust-native, no Flask recording exists; DECISIONS.md 2026-08-24). Strict-slash routing like every other rule in the app: both data rules are registered WITHOUT trailing slashes, so the slash variant 404s with the JSON envelope and no redirect (GET /api/ health stays the only slash-registered rule). Same 404 with or without credentials — routing misses before auth. POST /api/import/data/ 404s identically."
}
