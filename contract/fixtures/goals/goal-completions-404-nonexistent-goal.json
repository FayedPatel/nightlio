{
  "method": "GET",
  "path": "/api/goals/999999/completions",
  "request": {
    "headers_that_matter": {
      "Authorization": "Bearer <JWT>"
    },
    "body": null
  },
  "response": {
    "status": 404,
    "content_type": "application/json",
    "body": {
      "error": "Not found"
    }
  },
  "notes": "contract change (owner-approved status-code consistency fix). Flask returned 200 [] here (the query simply matched no rows, no ownership/existence check); the Rust port now 404s with the same body GET /api/goals/{id} uses for a missing/foreign goal, identically on all alias paths (/goals/{id}/completions[/], /goal/{id}/completions[/]). An existing goal with no completions still returns 200 [] (goal-completions-200-empty.json). Filename kept for history; the recorded status is now 404."
}
