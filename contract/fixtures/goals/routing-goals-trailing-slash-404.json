{
  "method": "GET",
  "path": "/api/goals/",
  "request": {
    "headers_that_matter": {
      "Authorization": "Bearer <JWT>"
    },
    "body": null
  },
  "response": {
    "status": 404,
    "content_type": "application/json",
    "body": {
      "error": "Resource not found"
    }
  },
  "notes": "ROUTING: /api/goals is registered with default strict_slashes=True; Flask 404s the trailing-slash variant on GET (no 308 redirect observed). The global 404 handler makes the body JSON {\"error\": \"Resource not found\"}, not HTML. Status 404 + JSON error shape is the contract."
}
