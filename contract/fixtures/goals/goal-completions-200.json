{
  "method": "GET",
  "path": "/api/goals/1/completions",
  "request": {
    "headers_that_matter": {
      "Authorization": "Bearer <JWT>"
    },
    "body": null
  },
  "response": {
    "status": 200,
    "content_type": "application/json",
    "body": [
      {
        "date": "<DATE>"
      },
      {
        "date": "<DATE>"
      }
    ]
  },
  "notes": "Bare array of {date} objects, ascending. Default window is the last 90 days when start/end query params are absent. Volatile values normalized: 'YYYY-MM-DD HH:MM:SS' timestamps -> <TS>, bare 'YYYY-MM-DD' dates -> <DATE>. Autoincrement ids kept as recorded (first rows of a fresh database)."
}
