{
  "method": "OPTIONS",
  "path": "/api/i18n/languages",
  "request": {
    "headers_that_matter": {},
    "body": null
  },
  "response": {
    "status": 204,
    "content_type": "text/html; charset=utf-8",
    "body": "",
    "headers_that_matter": {
      "Allow": "HEAD, GET, OPTIONS"
    }
  },
  "note": "NEW FAMILY (v0.6.0, hand-authored spec fixture — no Flask recording exists; DECISIONS.md 2026-08-22). App-wide unified OPTIONS: 204, empty body, Allow header (Flask-style comma-space join via the shared automatic_options handler), Content-Type text/html; charset=utf-8 kept, no Content-Length on the wire. Answered before any handler logic — an empty pack cache changes nothing. A CORS preflight (Origin + Access-Control-Request-Method) rides the same 204 with the Access-Control-* headers appended by the after-request CORS layer, exactly as misc/preferences_options_204_cors_preflight.json records. OPTIONS /api/i18n/es answers identically."
}
