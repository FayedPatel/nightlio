{
  "method": "POST",
  "path": "/api/statistics/view/",
  "request": {
    "headers_that_matter": {
      "Authorization": "Bearer <JWT>"
    },
    "body": null
  },
  "response": {
    "status": 404,
    "content_type": "application/json",
    "body": {
      "error": "Resource not found"
    }
  },
  "notes": "contract change (new endpoint): the rule is registered without a trailing slash (strict-slash default), so /api/statistics/view/ is a 404 with the app-level JSON body — no redirect. Routing fails before auth runs."
}
