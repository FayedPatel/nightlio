{
  "method": "GET",
  "path": "/api/mood/9999/selections",
  "request": {
    "headers_that_matter": {
      "Authorization": "Bearer <JWT>"
    },
    "body": null
  },
  "response": {
    "status": 404,
    "content_type": "application/json",
    "body": {
      "error": "Entry not found"
    }
  },
  "notes": "contract change (owner-approved status-code consistency fix). Flask returned 200 [] here (mood_service.get_entry_selections returns [] when ownership verification fails); the Rust port now 404s with the same body GET /api/mood/{id} uses for a nonexistent or foreign entry. An entry that EXISTS but has no selections (including after a group-delete cascade) still returns 200 []."
}
