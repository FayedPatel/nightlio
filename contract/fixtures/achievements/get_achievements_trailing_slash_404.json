{
  "method": "GET",
  "path": "/api/achievements/",
  "request": {
    "headers_that_matter": {
      "Authorization": "Bearer <JWT>"
    },
    "body": null
  },
  "response": {
    "status": 404,
    "content_type": "application/json",
    "body": {
      "error": "Resource not found"
    }
  },
  "notes": "Rule is registered WITHOUT a trailing slash and Flask/Werkzeug strict_slashes therefore 404s the slashed form (no redirect). The app's global 404 handler (api/utils/error_handlers.py) converts it to JSON {\"error\": \"Resource not found\"} — not Werkzeug's default HTML page."
}
