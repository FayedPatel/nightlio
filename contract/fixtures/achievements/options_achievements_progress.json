{
  "method": "OPTIONS",
  "path": "/api/achievements/progress",
  "request": {
    "headers_that_matter": {},
    "body": null
  },
  "response": {
    "status": 204,
    "content_type": "text/html; charset=utf-8",
    "body": ""
  },
  "notes": "CORS preflight: unauthenticated OPTIONS returns 204 with an empty body. No auth required. contract change: the dedicated OPTIONS handler is gone — this is now the same app-wide automatic OPTIONS as every other rule, so the response also carries an Allow header (HEAD, GET, OPTIONS); the graded headers above do not list Allow, so the recorded body/status stay valid."
}
